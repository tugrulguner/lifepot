# Optional. Kept server-side by src/app/api/judge/route.ts.
# Local development needs only TYPESAFE_API_KEY.
# Production additionally requires the two Upstash values for abuse protection.
TYPESAFE_API_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
