# Optional. Kept server-side by src/app/api/judge/route.ts.
# Paid Jev calls are enabled only when all three variables are present.
TYPESAFE_API_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
