import { describe,expect,it } from "vitest"; import { deterministicSetup } from "./setup";describe("setup",()=>{it("interprets predators as a founder guild, never a hazard",()=>{const c=deterministicSetup({world:"rich prey",threat:"predator packs in waves",reward:"diverse armored coexistence"});expect(c.environment.hazard).not.toBe("predator");expect(c.founders.predatorStrategy).toBe("pack_hunting");expect(c.founders.diversity).toBe("varied");});it("uses deterministic founder balances",()=>expect(deterministicSetup({world:"prey heavy",threat:"drought",reward:"survive"}).founders.balance).toBe("prey_heavy"));});
